import 'package:flutter/material.dart';

class NreCheck extends StatelessWidget {
  const NreCheck({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
